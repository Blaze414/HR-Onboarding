// Imported once, for its side effect of setting `globalThis.pdfjsWorker` —
// see the comment in DocumentPreview.tsx. Not typed by pdfjs-dist itself.
declare module 'pdfjs-dist/legacy/build/pdf.worker.min.mjs';
